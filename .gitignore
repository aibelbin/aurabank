# dependencies
node_modules/

# next
.next/
out/
next-env.d.ts
*.tsbuildinfo

# local data — the waitlist database lives here and is never committed
apps/landing/data/*
!apps/landing/data/.gitkeep

# database snapshots written by the backup sidecar on the deployment host
backups/

# env
.env
.env.*
!.env.example

# os
.DS_Store
